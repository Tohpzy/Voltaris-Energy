# Voltaris Energy — Website Files

- `index.html` + `styles.css` + `script.js` — the plain HTML/CSS/JS version. Open `index.html` directly in a browser, or serve the folder with any static host.
- `App.jsx` — a React component version of the same site (same design, same `styles.css`), using state instead of direct DOM manipulation for page routing, tabs, the FAQ accordion, and forms. Drop it into a React (e.g. Vite or Create React App) project as `App.jsx`, import `styles.css` alongside it, and render `<App />`.

Update the placeholder phone numbers, email, address and WhatsApp link (`wa.me/2348000000000`) with real details before launch.
